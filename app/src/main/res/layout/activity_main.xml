<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HalamanUtama.MainActivity"
    android:id="@+id/layout">


    <TextView
        android:id="@+id/textLocation"
        android:layout_width="wrap_content"
        android:layout_height="70dp"
        android:layout_alignParentStart="true"
        android:layout_marginStart="15dp"
        android:layout_marginBottom="20dp"
        android:text="Searching Location"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="20dp" />

    <TextView
        android:id="@+id/textWeather"
        android:layout_width="wrap_content"
        android:layout_height="70dp"
        android:layout_alignBottom="@+id/textLocation"
        android:layout_marginStart="15dp"
        android:layout_marginBottom="1dp"
        android:layout_toEndOf="@+id/textLocation"
        android:text="Searching weather jogja"
        android:textAlignment="textEnd"
        android:textColor="@color/black"
        android:textSize="20dp"
        android:paddingRight="8dp"/>

    <androidx.recyclerview.widget.RecyclerView
        app:layout_constraintTop_toTopOf="@id/textLocation"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/itemnote"
        android:layout_marginTop="60dp"
        android:id="@+id/rvNotes"></androidx.recyclerview.widget.RecyclerView>

    <Button
        android:id="@+id/tombolTambah"
        android:layout_width="65dp"
        android:layout_height="65dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="40dp"
        android:layout_marginTop="40dp"
        android:layout_marginEnd="40dp"
        android:layout_marginBottom="40dp"
        android:background="@drawable/lingkaran"
        android:text="+"
        android:textColor="@color/white"
        android:textSize="20dp" />
</RelativeLayout>